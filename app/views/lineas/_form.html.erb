<%= form_for(@linea) do |f| %>
  <% if @linea.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@linea.errors.count, "error") %> prohibited this linea from being saved:</h2>

      <ul>
      <% @linea.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :factura_id %><br>
    <%= f.number_field :factura_id %>
  </div>
  <div class="field">
    <%= f.label :concepto_id %><br>
    <%= f.number_field :concepto_id %>
  </div>
  <div class="field">
    <%= f.label :importe %><br>
    <%= f.text_field :importe %>
  </div>
  <div class="field">
    <%= f.label :leyenda %><br>
    <%= f.text_field :leyenda %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
